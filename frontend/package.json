{
  "name": "frontend",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "katex": "^0.17.0",
    "sass-embedded": "^1.100.0"
  },
  "scripts": {
    "build:wasm": "cp -r ./node_modules/katex/dist/fonts public/fonts && wasm-pack build --target web --out-dir ./frontend/build/pkg",
    "dev": "bun run build:wasm && vite",
    "build": "bun run build:wasm && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "mathlive": "^0.110.0",
    "typescript": "^5",
    "vite": "^8.1.5"
  }
}
